#!ipxe
# Boot a persistent RancherOS to RAM

# Location of Kernel/Initrd images
set base-url http://releases.rancher.com/os/latest

kernel ${base-url}/vmlinuz rancher.state.dev=LABEL=RANCHER_STATE rancher.state.wait rancher.cloud_init.datasources=[url:https://raw.githubusercontent.com/RAM9/farmkun/refs/heads/main/rcloud-config]
initrd ${base-url}/initrd
boot
#use 'ros install' to install to disk.
